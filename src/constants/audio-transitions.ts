/** Crossfade when mood pipeline returns new soundscape params (aligned with Tone ramp duration). */
export const MOOD_CROSSFADE_SECONDS = 14;

/** Palette preset shifts — shorter than full mood regeneration. */
export const PALETTE_CROSSFADE_SECONDS = 8;
